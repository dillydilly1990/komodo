services:
  zurg:
    image: ghcr.io/debridmediamanager/zurg-testing:latest    
    container_name: zurg
    restart: unless-stopped
    environment:
      TZ: America/Detroit
      PUID: 100
      PGID: 99
    ports:
      - 9999:9999
    volumes:
      - ${APPDATA:?err}/debrid/core/zurg-testing/scripts/plex_update.sh:/app/plex_update.sh
      - ${APPDATA:?err}/debrid/core/zurg-testing/config.yml:/app/config.yml
      - ${APPDATA:?err}/debrid/core/zurg-testing/zurgdata:/app/data

  rclone:
    image: rclone/rclone:latest
    container_name: rclone
    restart: unless-stopped
    environment:
      TZ: America/Detroit
      PUID: 100
      PGID: 99
    volumes:
      - ${ZURGMNT:?err}:/data:rshared # CHANGE /mnt/zurg WITH YOUR PREFERRED MOUNT PATH
      - ${APPDATA:?err}/debrid/core/zurg-testing/rclone.conf:/config/rclone/rclone.conf
    cap_add:
      - SYS_ADMIN
    security_opt:
      - apparmor:unconfined
    devices:
      - /dev/fuse:/dev/fuse:rwm
    depends_on:
      - zurg
    command: "mount zurg: /data --allow-other --allow-non-empty --dir-cache-time 10s --vfs-cache-mode full"
